(()=>{const c=window.APP_CONFIG||{},base=(c.SUPABASE_URL||'').replace(/\/$/,''),key=c.SUPABASE_ANON_KEY||'';
async function page(t,{select='*',filters={},order='',limit=1000,offset=0}={}){if(!base||!key)throw Error('Supabase config hiányzik');const u=new URL(`${base}/rest/v1/${t}`);u.searchParams.set('select',select);u.searchParams.set('limit',limit);u.searchParams.set('offset',offset);if(order)u.searchParams.set('order',order);Object.entries(filters).forEach(([k,v])=>v!==''&&v!=null&&u.searchParams.set(k,v));const headers={apikey:key};if(!key.startsWith('sb_'))headers.Authorization=`Bearer ${key}`;const r=await fetch(u,{headers});if(!r.ok)throw Error(`${t}: HTTP ${r.status}`);return r.json()}
async function all(t,o={}){let out=[],off=0;for(;;off+=1000){const b=await page(t,{...o,limit:1000,offset:off});out.push(...b);if(b.length<1000)return out}}
const pct=(v,d=1)=>v==null||Number.isNaN(+v)?'—':`${(+v*100).toFixed(d)}%`,num=(v,d=2)=>v==null||Number.isNaN(+v)?'—':(+v).toLocaleString('hu-HU',{maximumFractionDigits:d}),huf=v=>{if(v==null)return'—';let n=+v,a=Math.abs(n);return a>=1e12?`${(n/1e12).toFixed(2)} ezermrd Ft`:a>=1e9?`${(n/1e9).toFixed(1)} mrd Ft`:a>=1e6?`${(n/1e6).toFixed(1)} M Ft`:`${Math.round(n).toLocaleString('hu-HU')} Ft`},days=v=>v==null?'—':+v>=730?`${(+v/365.2425).toFixed(1)} év`:+v>=60?`${(+v/30.4375).toFixed(1)} hó`:`${Math.round(+v)} nap`,esc=s=>String(s??'').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x])),cls=v=>v==null?'':+v>0.00005?'pos':+v<-.00005?'neg':'',median=a=>{a=a.filter(Number.isFinite).sort((x,y)=>x-y);if(!a.length)return null;let m=Math.floor(a.length/2);return a.length%2?a[m]:(a[m-1]+a[m])/2},down=(a,n=1000)=>a.length<=n?a:Array.from({length:n},(_,i)=>a[Math.round(i*(a.length-1)/(n-1))]);
window.App={page,all,pct,num,huf,days,esc,cls,median,down};})();
